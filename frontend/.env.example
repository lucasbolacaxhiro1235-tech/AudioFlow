# URL base da API. Em produção (Cloudflare Pages), aponte para o seu backend.
# Ex.: https://api.audioflow.pages.dev
# Em desenvolvimento local, deixe vazio para usar o proxy do Vite (/api -> localhost:8000).
VITE_API_BASE=